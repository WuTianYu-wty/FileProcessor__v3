# ⚠️ 重要提示 - 功能使用前必读

## 🔴 当前已知问题

### 1. 文档转换功能无法使用
**原因**：缺少 Python 依赖库

**解决方法**：
```bash
# 方法1：双击运行（推荐）
安装Python依赖.bat

# 方法2：手动安装
cd python-scripts
pip install -r requirements.txt
```

**需要安装的依赖**：
- python-docx (Word处理)
- python-pptx (PPT处理)
- openpyxl (Excel处理)
- PyMuPDF (PDF处理)

### 2. 文件名乱码问题
**原因**：数据库中文件名编码问题

**临时解决方法**：
- 文件实际存储正常，只是显示乱码
- 功能不受影响，可以正常转换
- 建议：上传新文件时使用英文文件名

**永久解决方法**（需要重建数据库）：
```bash
# 备份现有数据
# 删除 backend/database/fileprocessor.db
# 重新启动服务，会自动创建新数据库
```

---

## ✅ 已修复的功能

### 1. 真正的全选功能
- ✅ 文档转换器：点击"选择全部文件"可选择所有文件（不受分页限制）
- ✅ OCR识别：点击"选择全部文件"可选择所有支持的文件
- ✅ 添加"清空所有"快捷操作

### 2. 表格勾选框方式
- ✅ 文档转换：使用表格勾选，更直观
- ✅ OCR识别：使用表格勾选，支持批量
- ✅ 支持全选、反选、清空

### 3. 批量处理
- ✅ 可以同时选择多个文件进行处理
- ✅ 显示处理进度和结果
- ✅ 自动跳过失败的文件

---

## 🚀 使用步骤

### 第一次使用文档转换功能：

1. **安装Python依赖**
   ```bash
   双击运行：安装Python依赖.bat
   ```
   或者手动运行：
   ```bash
   cd python-scripts
   pip install -r requirements.txt
   ```

2. **重启服务**
   - 按 Ctrl+C 停止当前服务
   - 重新运行 `node start-dev-unified.js`

3. **测试功能**
   - 打开文档格式转换页面
   - 选择转换类型（如：PDF → Word）
   - 勾选要转换的文件
   - 点击"开始转换"

---

## 📝 功能说明

### 全选功能
1. **当前页全选**：勾选框勾选当前页的所有文件
2. **真正全选**：点击下拉菜单中的"选择全部文件"可选择所有文件

### 文档转换支持的格式
- PDF → Word
- Word → PDF
- Excel → PDF
- PPT → PDF

### OCR识别支持的文件
- PDF文件
- 图片文件（JPG, PNG, JPEG）

---

## 🔧 故障排除

### 如果文档转换仍然失败：

1. **检查Python版本**
   ```bash
   python --version
   # 需要 Python 3.8+
   ```

2. **检查依赖是否安装成功**
   ```bash
   pip list | findstr docx
   pip list | findstr PyMuPDF
   ```

3. **查看详细错误**
   - 打开浏览器开发者工具 (F12)
   - 查看 Console 标签页的错误信息
   - 或查看后端终端输出

### 如果OCR识别失败：

1. **首次使用会下载模型**
   - PaddleOCR模型较大（~200MB）
   - 请耐心等待下载完成

2. **GPU加速问题**
   - 如果没有NVIDIA显卡，关闭GPU加速选项
   - 使用CPU模式也能正常工作

---

## 📞 技术支持

如有问题，请提供：
1. 浏览器控制台的错误信息（F12）
2. 后端终端的错误日志
3. 操作步骤截图

**开发团队：WuTianyu**

