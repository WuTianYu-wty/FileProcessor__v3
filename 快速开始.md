# 🚀 FileProcessor 快速启动指南

## 📋 前置条件

在开始之前，请确保已安装以下软件：

- ✅ **Node.js** (v16.x 或更高) - [下载地址](https://nodejs.org/)
- ✅ **Python** (v3.8 或更高) - [下载地址](https://www.python.org/)
- ✅ **npm** 或 **yarn** 包管理器

## ⚡ 快速启动（推荐）

### Windows 用户

**方式 1: PowerShell（推荐）**
```powershell
.\start-dev.ps1
```
中文界面，无乱码，自动检查依赖

**方式 2: 批处理文件**
```bash
.\start-dev-cn.bat  # 中文版 (GBK 编码)
.\start-dev.bat     # 英文版 (UTF-8 编码)
```

### Linux/Mac 用户

```bash
chmod +x start-dev.sh
./start-dev.sh
```

## 📦 手动启动

如果自动启动脚本无法运行，请按以下步骤手动启动：

### 步骤 1: 安装依赖

```bash
# 1. 安装后端依赖
cd backend
npm install

# 2. 安装前端依赖
cd ../frontend
npm install

# 3. 安装 Python 依赖（可选，OCR 功能需要）
cd ../python-scripts
pip install -r requirements.txt
```

### 步骤 2: 启动服务

**打开两个终端窗口：**

**终端 1 - 启动后端**
```bash
cd backend
npm run dev
```
✅ 后端将在 http://localhost:3000 运行

**终端 2 - 启动前端**
```bash
cd frontend
npm run dev
```
✅ 前端将在 http://localhost:5173 运行

### 步骤 3: 访问应用

打开浏览器访问: **http://localhost:5173**

## 🎯 功能测试

启动成功后，可以测试以下功能：

### 1. 仪表板
- 查看文件统计信息
- 查看最近上传的文件

### 2. 文件上传
- 点击或拖拽上传文件
- 支持格式: PDF, Word (.doc, .docx), TXT, WPS
- 单个文件最大 5GB

### 3. 文件列表
- 查看所有已上传的文件
- 搜索和过滤文件
- 下载或删除文件

### 4. PDF 工具（开发中）
- OCR 识别与脱敏
- PDF 拆分
- PDF 合并

### 5. 系统设置
- 配置数据保留天数
- 设置文件大小限制
- 查看系统信息

## 🔍 验证安装

### 检查后端是否正常运行

访问健康检查接口: http://localhost:3000/api/health

应该返回:
```json
{
  "status": "ok",
  "timestamp": "2025-12-05T...",
  "environment": "development"
}
```

### 检查前端是否正常运行

访问: http://localhost:5173

应该看到 FileProcessor 的主界面。

## 🐛 常见问题

### 问题 1: 端口被占用

**错误信息**: `Port 3000 is already in use`

**解决方案**:
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Linux/Mac
lsof -ti:3000 | xargs kill -9
```

### 问题 2: npm 安装依赖失败

**解决方案**:
```bash
# 清除 npm 缓存
npm cache clean --force

# 删除 node_modules 重新安装
rm -rf node_modules
npm install
```

### 问题 3: Python 依赖安装失败

**解决方案**:
```bash
# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或使用阿里云镜像
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
```

### 问题 4: better-sqlite3 编译失败

**错误信息**: `gyp ERR! find VS You need to install the latest version of Visual Studio`

**解决方案**:
```bash
# 已修复：降级到 better-sqlite3@9.6.0（有预编译二进制文件）
# 如果仍有问题，删除 node_modules 重新安装
cd backend
rm -rf node_modules package-lock.json
npm install
```

### 问题 5: PowerShell 执行策略限制

**错误信息**: `无法加载文件 D:\nodeJs\npm.ps1，因为在此系统上禁止运行脚本`

**原因**: Windows PowerShell 默认禁止运行脚本，需要修改执行策略。

**解决方案**:

**步骤 1**: 以管理员身份运行 PowerShell
- 按 `Win + S` 搜索 "PowerShell"
- 右键点击 "Windows PowerShell"
- 选择 **"以管理员身份运行"**

**步骤 2**: 执行以下命令
```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**步骤 3**: 确认提示
- 输入 `Y` 并按 Enter

**步骤 4**: 验证设置
```powershell
Get-ExecutionPolicy -Scope CurrentUser
# 应该显示: RemoteSigned
```

**步骤 5**: 关闭管理员窗口，打开新的 PowerShell 窗口，重新运行命令

**如果没有管理员权限**:
```powershell
# 在当前窗口临时允许（关闭窗口后失效）
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

详细说明请查看: [INSTALL_VS_GUIDE.md](INSTALL_VS_GUIDE.md)

### 问题 6: 批处理文件中文乱码

**解决方案**: 使用 PowerShell 脚本（推荐）
```powershell
.\start-dev.ps1
```
或使用 GBK 编码的批处理文件：
```bash
.\start-dev-cn.bat
```

### 问题 7: PaddleOCR 首次运行慢

**原因**: 首次运行需要下载 OCR 模型文件（约 100MB）

**解决方案**: 耐心等待，模型下载完成后会自动缓存。

## 📁 项目结构

```
FileProcessor_v3/
├── backend/           # 后端服务 (Node.js + Express)
├── frontend/          # 前端应用 (React + Vite)
├── python-scripts/    # Python 处理脚本
├── uploads/           # 文件上传目录
└── dev-logs/          # 开发文档
```

## 🔧 配置文件

### 后端配置

创建 `backend/.env` 文件（可选）:
```env
PORT=3000
NODE_ENV=development
DB_PATH=./data/fileprocessor.db
UPLOAD_DIR=../uploads
MAX_FILE_SIZE=52428800
RETENTION_DAYS=7
LOG_DIR=./logs
LOG_LEVEL=info
```

### 前端代理配置

`frontend/vite.config.js` 已配置代理:
```javascript
proxy: {
  '/api': {
    target: 'http://localhost:3000',
    changeOrigin: true
  }
}
```

## 📊 系统要求

### 最低配置
- CPU: 双核 2.0 GHz
- 内存: 4 GB RAM
- 磁盘: 2 GB 可用空间

### 推荐配置
- CPU: 四核 2.5 GHz 或更高
- 内存: 8 GB RAM 或更高
- 磁盘: 10 GB 可用空间（用于文件存储）

## 🎓 学习资源

### 相关文档
- 📖 [项目说明](README.md)
- 📝 [开发总结](dev-logs/DEVELOPMENT_SUMMARY.md)
- 🔬 [Python 脚本文档](python-scripts/README.md)
- 📋 [实施计划](dev-logs/implementation_plan.md.resolved)

### API 文档

#### 文件管理
- `GET /api/files` - 获取文件列表
- `POST /api/files/upload` - 上传文件
- `GET /api/files/:id/download` - 下载文件
- `DELETE /api/files/:id` - 删除文件

#### 系统管理
- `GET /api/health` - 健康检查
- `GET /api/logs` - 获取日志
- `GET /api/logs/retention-check` - 数据保留检查

## 🛠️ 开发模式

启动开发模式后：

- 后端支持热重载 (nodemon)
- 前端支持 HMR (Vite)
- 修改代码后自动刷新

## 🚪 停止服务

### Windows
关闭启动的命令行窗口，或按 `Ctrl + C`

### Linux/Mac
在启动脚本的终端按 `Enter` 键，或按 `Ctrl + C`

## 📞 获取帮助

如果遇到问题：

1. 查看 [常见问题](#常见问题) 部分
2. 检查终端日志输出
3. 查看 `backend/logs/` 目录下的日志文件
4. 阅读 [开发总结文档](dev-logs/DEVELOPMENT_SUMMARY.md)

## 🎉 下一步

项目启动成功后，你可以：

1. 📤 **上传文件** - 测试文件上传功能
2. 📂 **浏览文件** - 查看文件列表
3. ⚙️ **配置系统** - 在设置页面调整参数
4. 🔍 **查看日志** - 了解系统运行状态

---

**祝您使用愉快！** 🎊

如有问题，请参考 [README.md](README.md) 或查看开发文档。

