# FileProcessor - 本地文件处理 Web 应用

一个功能强大的本地部署文件处理工具，支持文件管理、OCR 识别、脱敏处理、PDF 拆分与合并等功能。

## ✨ 主要功能

- 📁 **文件管理**: 支持 Word, WPS, TXT, PDF 等多种格式的上传与管理
- 🔍 **OCR 识别**: 集成 PaddleOCR，支持中文文字识别
- 🔒 **智能脱敏**: 自动识别并脱敏敏感信息（姓名、手机号、身份证、车牌等）
- ✂️ **PDF 工具**: 支持 PDF 按页拆分、按目录拆分、多文件合并
- 🗂️ **自动化运维**: 日志自动轮转（1小时）、数据保留策略（7天提醒）
- 🎨 **现代化 UI**: 基于 Ant Design 的美观界面

## 🛠️ 技术栈

### 后端
- **运行时**: Node.js
- **Web 框架**: Express.js
- **数据库**: SQLite (better-sqlite3)
- **日志**: Winston + winston-daily-rotate-file
- **文件上传**: Multer

### 前端
- **框架**: React 18
- **构建工具**: Vite
- **UI 组件库**: Ant Design 5
- **路由**: React Router 6
- **HTTP 客户端**: Axios

### Python 脚本
- **PDF 处理**: PyMuPDF (fitz)
- **OCR 引擎**: PaddleOCR
- **图像处理**: Pillow, OpenCV

## 📦 安装与运行

### 前置要求

- Node.js 16.x 或更高版本
- Python 3.8 或更高版本
- npm 或 yarn

### 安装步骤

1. **克隆项目**
```bash
git clone <repository-url>
cd FileProcessor_v3
```

2. **安装后端依赖**
```bash
cd backend
npm install
```

3. **安装前端依赖**
```bash
cd ../frontend
npm install
```

4. **安装 Python 依赖**
```bash
cd ../python-scripts
pip install -r requirements.txt
```

### 运行项目

**推荐方式 - 一键启动：**

**Windows (PowerShell):**
```powershell
.\start-dev.ps1
```

**Windows (批处理):**
```bash
.\start-dev-cn.bat  # 中文版
# 或
.\start-dev.bat     # 英文版
```

**Linux/Mac:**
```bash
chmod +x start-dev.sh
./start-dev.sh
```

---

**手动启动方式：**

1. **启动后端服务**
```bash
cd backend
npm run dev
# 或者使用生产模式: npm start
```
后端将在 http://localhost:3000 运行

2. **启动前端开发服务器**
```bash
cd frontend
npm run dev
```
前端将在 http://localhost:5173 运行

3. **访问应用**

打开浏览器访问: http://localhost:5173

## 📁 项目结构

```
FileProcessor_v3/
├── backend/              # 后端 Node.js 应用
│   ├── routes/          # API 路由
│   ├── database/        # 数据库模块
│   ├── utils/           # 工具函数
│   ├── data/            # SQLite 数据库文件
│   ├── logs/            # 日志文件
│   └── server.js        # 入口文件
├── frontend/            # 前端 React 应用
│   ├── src/
│   │   ├── pages/      # 页面组件
│   │   ├── layouts/    # 布局组件
│   │   ├── App.jsx     # 主应用组件
│   │   └── main.jsx    # 入口文件
│   └── index.html
├── python-scripts/      # Python 处理脚本
│   ├── ocr_processor.py   # OCR 识别与脱敏
│   ├── pdf_split.py       # PDF 拆分
│   ├── pdf_merge.py       # PDF 合并
│   └── requirements.txt   # Python 依赖
├── uploads/             # 文件上传存储目录
└── dev-logs/            # 开发日志与文档
```

## 🎯 核心功能说明

### 文件上传
- 支持单文件和批量上传
- 文件类型: .pdf, .doc, .docx, .txt, .wps
- 单文件最大 5GB
- 拖拽上传支持

### OCR 识别与脱敏
- 自动识别 PDF 和图片中的文字
- 支持的敏感信息类型:
  - 手机号码
  - 身份证号
  - 电子邮箱
  - 车牌号码
- 可自定义脱敏规则

### PDF 工具
- **拆分**: 按页码范围或 PDF 目录（书签）拆分
- **合并**: 合并多个 PDF 文件，保留元数据

### 数据保留策略
- 默认 7 天保留期
- 自动检测超期文件并提醒
- 支持批量清理

## 🔧 配置说明

### 后端配置
编辑 `backend/.env`:
```env
PORT=3000
NODE_ENV=development
DB_PATH=./data/fileprocessor.db
UPLOAD_DIR=../uploads
MAX_FILE_SIZE=52428800
RETENTION_DAYS=7
LOG_DIR=./logs
LOG_LEVEL=info
```

### 前端配置
编辑 `frontend/vite.config.js` 中的代理设置以连接后端 API。

## 📝 API 文档

### 文件管理 API

- `GET /api/files` - 获取文件列表
- `GET /api/files/:id` - 获取单个文件信息
- `POST /api/files/upload` - 上传单个文件
- `POST /api/files/upload-multiple` - 批量上传
- `PUT /api/files/:id/status` - 更新文件状态
- `DELETE /api/files/:id` - 删除文件
- `GET /api/files/:id/download` - 下载文件

### 日志 API

- `GET /api/logs` - 获取系统日志
- `POST /api/logs` - 创建日志条目
- `GET /api/logs/retention-check` - 检查数据保留策略

### 健康检查

- `GET /api/health` - 服务器健康检查

## 🐛 已知问题

- OCR 首次运行需要下载模型文件（约 100MB）
- Python 脚本需要确保环境正确配置
- 大文件 OCR 处理可能耗时较长

## 🚀 后续开发计划

详见 `dev-logs/implementation_plan.md.resolved`

- [ ] Phase 2: 完善 OCR 和 PDF 处理功能
- [ ] Phase 3: 前端功能增强
- [ ] Phase 4: 性能优化与测试

## 📄 许可证

MIT License

## 👥 贡献者

Antigravity (AI Assistant)

## 📞 支持

如有问题，请查看开发日志或提交 Issue。

---

**注意**: 本项目为本地部署应用，适合个人或团队内部使用。请确保遵守数据安全和隐私保护相关规定。

