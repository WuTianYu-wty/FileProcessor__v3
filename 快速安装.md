# 快速安装Python依赖

## 方法1：直接复制命令（最简单）

打开cmd或PowerShell，复制以下命令：

```bash
cd D:\CODE\FileProcessor_v3\python-scripts
pip install -r requirements-minimal.txt
```

## 方法2：使用国内镜像（更快）

```bash
cd D:\CODE\FileProcessor_v3\python-scripts
pip install -r requirements-minimal.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 方法3：运行批处理文件

双击运行：`install-deps.bat`

---

## 安装内容

最小化依赖（仅文档转换功能）：
- PyMuPDF (PDF处理)
- python-docx (Word处理)  
- python-pptx (PPT处理)
- openpyxl (Excel处理)
- Pillow (图片处理)

---

## 安装后

1. 重启开发服务器
2. 刷新浏览器页面
3. 测试文档转换功能

---

## 故障排除

### 如果提示pip版本过旧：
```bash
python -m pip install --upgrade pip
```

### 如果网络慢：
使用清华大学镜像源（方法2）

### 如果Python版本不对：
需要Python 3.8或更高版本
```bash
python --version
```

